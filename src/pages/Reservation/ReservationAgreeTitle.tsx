import { Text } from '@chakra-ui/react';

const ReservationAgreeTitle = () => {
  return (
    <Text fontSize="1rem" ml="2rem" fontWeight="bold">
      약관 동의
    </Text>
  );
};

export default ReservationAgreeTitle;
